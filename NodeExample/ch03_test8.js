var names = ['generaton', 'girlsday', 'tara'];

var users = [{name:'generaton', age:20}, {name:'girlsday', age:22}];

users.push({name:'tara', age:21});

console.log('user numbers : ' + users.length);
console.log('user first name' + users[0].name);
